<% provide(:title, @pet.name) %>
<%= render 'layouts/header' %>
<p>
  <strong>Name:</strong>
  <%= @pet.name %>
</p>

<p>
  <strong>Owner:</strong>
  <%= @pet.user.name %>
</p>

<%= link_to 'Edit', edit_pet_path(@pet) %> 

<%= render 'layouts/footer' %>
